<%- include('../partials/header', { title: 'Donations - Ella Rises', user: typeof user !== 'undefined' ? user : null }) %>

<div class="page-header">
    <h1>Donations</h1>
    <p>View donation records (View Only)</p>
</div>

<div class="search-container">
    <label for="search-input" class="sr-only">Search donations</label>
    <input type="text" id="search-input" class="search-input" placeholder="Search donations by donor name, email, or amount..." aria-label="Search donations by donor name, email, or amount">
</div>

<div class="table-container" role="region" aria-label="Donations table">
    <table aria-label="Donations">
        <thead>
            <tr>
                <th scope="col">Donor Name</th>
                <th scope="col">Email</th>
                <th scope="col">Amount</th>
                <th scope="col">Date</th>
            </tr>
        </thead>
        <tbody id="donations-table-body">
            <% if (typeof donations !== 'undefined' && donations.length > 0) { %>
                <% donations.forEach(function(donation) { %>
                    <tr>
                        <td>
                            <% 
                                const donorName = (donation.FirstName || donation.firstname || donation.firstName || '') + ' ' + (donation.LastName || donation.lastname || donation.lastName || '');
                                const displayName = donorName.trim() || 'Unknown';
                            %>
                            <%= displayName %>
                        </td>
                        <td><%= (donation.Email || donation.email || '').trim() || '' %></td>
                        <td>$<%= parseFloat(donation.DonationAmount || donation.donationamount || donation.amount || 0).toFixed(2) %></td>
                        <td>
                            <% 
                                const donationDate = donation.DonationDate || donation.donationdate;
                                if (donationDate) {
                                    try {
                                        const date = new Date(donationDate);
                                        const formattedDate = date.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' });
                            %>
                                <%= formattedDate %>
                            <%  } catch(e) { %>
                                <%= donationDate %>
                            <%  }
                                } else { %>
                                N/A
                            <% } %>
                        </td>
                    </tr>
                <% }); %>
            <% } else { %>
                <tr>
                    <td colspan="4" class="text-center" role="status" aria-live="polite">No donations found.</td>
                </tr>
            <% } %>
        </tbody>
    </table>
</div>

<script>
    // Search functionality with screen reader support
    document.getElementById('search-input').addEventListener('input', function(e) {
        const searchTerm = e.target.value.toLowerCase();
        const rows = document.querySelectorAll('#donations-table-body tr');
        let visibleCount = 0;
        
        rows.forEach(row => {
            const text = row.textContent.toLowerCase();
            const isVisible = text.includes(searchTerm);
            row.style.display = isVisible ? '' : 'none';
            if (isVisible) visibleCount++;
        });
        
        const status = document.querySelector('#donations-table-body [role="status"]');
        if (status && visibleCount > 0) {
            status.textContent = `${visibleCount} donation${visibleCount === 1 ? '' : 's'} found`;
        }
    });
</script>

<%- include('../partials/footer') %>


